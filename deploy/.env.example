# Docker Compose 环境变量示例
# 用法：复制为 .env 并填入真实值（docker-compose 会自动读取同目录 .env）
# 位置：deploy/.env
# ⚠️ .env 已被 .gitignore 忽略，不会提交到仓库；本文件只是模板。

# 阿里云百炼 / 通义千问 API Key（AI 模块必须，否则 /ai 接口不可用、AI 容器可能反复重启）
DASHSCOPE_API_KEY=your-real-dashscope-api-key

# 生产数据库密码（应用数据源 SPRING_DATASOURCE_PASSWORD 与 MySQL root 共用同一密码）
# 请改成你自己的强密码，并同步修改服务器上 deploy/.env 的对应值
MYSQL_ROOT_PASSWORD=change_this_to_a_strong_password
SPRING_DATASOURCE_PASSWORD=change_this_to_a_strong_password
